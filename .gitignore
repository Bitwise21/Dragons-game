#include <bits/stdc++.h>
using namespace std;
int main () {
int a,b;
cin>>a>>b;
vector <pair <int,int>> str(b);

for(int i = 0; i < b; i++){
    cin>>str[i].first>>str[i].second;
}
sort(str.begin(),str.end());
for(int i = 0; i < b; i++){
    if(a>str[i].first){
        a+=str[i].second;
    }else {cout<<"NO"<<endl;
    return 0;
    }

}
cout<<"YES";
return 0;
}
